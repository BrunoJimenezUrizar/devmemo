<div id="role-options-<%= user.id %>" class="modal fade in" style="display: none;">
  <%= simple_form_for user, :url => "users/#{user.id}/update_roles_user", :html => {:method => :put, :class => 'form-horizontal' } do |f| %>
    <div class="modal-header">
      <a class="close" data-dismiss="modal">&#215;</a>
      <h3>Cambiar Role</h3>
    </div>
    <div class="modal-body">
      <%= f.input :role_ids, :collection => Role.all, :as => :radio_buttons, :label_method => lambda {|t| t.name.titleize}, :label => false, :item_wrapper_class => 'inline', checked: user.role_ids.first  %>
    </div>
    <div class="modal-footer">
      <%= f.submit "Cambiar Role", :class => "btn" %>
      <a class="btn" data-dismiss="modal" href="#">Close</a>
    </div>
  <% end %>
</div>
  <!--%Vista de la barra de navegación del usuario%-->
  <% if user_signed_in? %>
    <li class="dropdown"><a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= current_user.email %><b class="caret"></b></a>
      <ul class="dropdown-menu">
        <!--link para editar la cuenta personal-->
        <li><%= link_to t("helpers.actions.edit_account"), main_app.edit_current_user_users_path %>
        <li><%= link_to t("helpers.actions.edit_password_current_user_users"), main_app.edit_password_current_user_users_path %>
        <li><%=link_to t("helpers.actions.sign_out"), main_app.destroy_user_session_path, method: :delete%></li></li>
      </ul>
    </li>
    <!--link para desconectarse-->
  <% end %>